# نقطة البداية — أكاديمية وَرد (Single Source of Truth)

**النسخة:** 7 · **آخر تحديث:** 2026-06-24 (Code — مهاجرة المرحلة ١ · Claude الدردشة — قرار اللغة English-first)

> ## كيف تتعامل مع هذه الوثيقة
> | | |
> |---|---|
> | **الحالة** | 🟢 **حيّة (Living)** — نبضُ المشروع وفهرسُه؛ تُحدَّث باستمرار. |
> | **تملكها/تحدّثها** | **Claude Code** يصونها (الحالة + سجلّ الجلسات) في نهاية كلّ جلسة؛ و**Claude الدردشة** يسجّل فيها القراراتِ الاستراتيجية والمنهجية. |
> | **اقرأها أولاً** | أيُّ مساعدٍ (Code / Design / الدردشة) يقرأ هذا الملفّ **قبل أيّ عمل**. |
> | **تحوي** | الحالةَ الحالية · القراراتِ المثبّتة · الأدوارَ وطقسَ المزامنة · الحقائقَ التقنية الجارية · سجلَّ الجلسات. (نبضٌ وفهرس — لا تفاصيل.) |
> | **تشير إلى ثلاث وثائق** | 🧊 `Ward_Academy_Foundation.md` (الدستور) · 🧊 `Ward_Curriculum_Master_Reference.md` (المنهج والتقييم) · 🟢 `Ward_Academy_Design_System_Brief.md` (التصميم). التفصيلُ هناك، والنبضُ هنا. |

---

## ١) ما هو المشروع
أكاديمية وَرد — منصّة تعليم إنجليزية للأطفال **٩–١٣**، جلساتٌ فرديةٌ مباشرةٌ مع معلّم، يساعده ذكاءٌ اصطناعيّ **والمعلّم يعتمد كلّ مخرَجٍ قبل وصوله للطفل**. منهجٌ واحد: **«منهاج وَرد»** — منهجٌ خاصٌّ بالمنصّة مقاسٌ بمعيار **CEFR (A1→B1)** — ٣٠ وحدة · ٢١٥ هدفاً، مكتملٌ ومجمَّدٌ في `Ward_Curriculum_Master_Reference.md`. (أُلغي مسار «المنهج المدرسيّ» نهائياً 2026-06-20.)

## ٢) الحالة الحالية
**الواجهة التسويقية مبنيّةٌ بصرياً، والأساس (backend) + مساحات العمل الأربع (الإدارة/المعلّم/الطالب/وليّ الأمر) مبنيّةٌ وعاملةٌ ومُختبَرةٌ بعزلٍ حقيقي، ومنشورةٌ حيّاً على ward.academy (Vercel/fra1). حساب الإدارة مكتمل، و**حساب المعلّم (مساحة التعليم) مبنيٌّ بالكامل**: صفحة الطالب بسبعة تبويبات، محرّر خطّةٍ مرئيّ، جلساتٌ متكرّرة بفيديو داخليّ (Daily) وتقاريرُ مبنيّةٌ على نموذجٍ بسيطٍ تُشارَك على واتساب، واجباتٌ يدويّة/رقميّة، واختباراتُ وحدةٍ تُولَّد من الأهداف ويؤدّيها الطالب فيُصحَّح آلياً، ولوحةُ «مركز قيادة اليوم». وحسابا الطالب ووليّ الأمر يعكسان ذلك (نتائج الاختبارات + التقارير الكاملة + الخطّة بوحداتها).**

- ✅ **نظام التصميم** (Claude Design): مكتمل ومتماسك.
- ✅ **صفحة الهبوط**: منشورة على ward.academy، عربيةٌ افتراضيةٌ + RTL، فئة ٩–١٣. أُعيد بناؤها لتكون **مقودةً بالمنتج (تُري لا تُخبر)** بالأقسام: البطل (وردة تتفتّح عند الظهور + لمحة تقرير التفتّح) · كيف نعمل · إطلالة من الداخل (محاكاة تطبيق وليّ الأمر + الطالب) · تقرير التفتّح (الوردة = خريطة مهارات) · المعلّم الخاص (جلسة فردية) · دعم الذكاء الاصطناعي (محاكاة لوحة المعلّم: «لا يصل شيء للطالب قبل موافقتك») · النتائج (أرقام نموذجية) · الأسعار (٣ باقات، ميزات مشتركة) · آراء الأهل · الأسئلة الشائعة · الدعوة الأخيرة. مع: ظهور تدريجي عند التمرير، زرّ حجز ثابت على الجوّال، خلفية بطل ممتدّة، أزرار حجز خضراء، وصورة OG.
- ✅ **الصفحات الفرعية (محاكاة)**: `/enroll` (تسجيل ← حجز موعد ← اختبار مستوى، مع تحقّق من النموذج) · `/login` (دخول مرئيّ) · `/placement` (اختبار + نتيجة «بانتظار اعتماد المعلّم»).
- ✅ **الهوية واللغة**: البنفسجيّ بطل، الوردة هوية/الشرارة ذكاء، الخطوط مضبوطة، **أرقام غربية**، **بلا إيموجي**، **بلا أسماء كاملة** (أسماء أولى فقط في المحاكاة)، خطاب الذكاء الاصطناعي **مخفَّف للأهل** (التفاصيل للمعلّم).
- 🗑️ **مُزال**: صفحتا `/teacher` و`/brand` (لوحة المعلّم بُنيت حقيقيةً في `/studio`).
- ✅ **الأساس (backend) — مبنيّ ومُختبَر (٣٨ هجرة على Supabase/Frankfurt، RLS على كل جدول):**
  - **العزل والهوية:** `tenants` (وحدة العزل) · `profiles` (هوية + أدوارٌ بالطبقات) · خطّاف JWT يحقن `tenant_id`+`roles` (مفعّل). لا تصعيد أدوار (لا كتابة عميلٍ على `profiles`).
  - **أولياء الأمور والطلاب:** `guardianships` (واحد→متعدّد + موافقةٌ لكل ابن) · رؤية الملفّات مقيّدةٌ بالدور (وليّ الأمر يرى أبناءه فقط، المعلّم يرى مستأجِره).
  - **محرّك المحتوى:** `objectives` (CEFR فقط) · `items` (موسومةٌ بالهدف) + **بوّابة اعتماد المعلّم** · `item_keys` (الإجابة محجوبةٌ عن الطالب) · `assignments` (إسنادٌ لكل طالب).
  - **الإرسالات والتقدّم:** `submissions` (تصحيحٌ موثوقٌ على الخادم، لا تزوير) · `progress_records` (عدّاتٌ صادقة، تحديثٌ متزامنٌ بـ trigger، الإتمام منفصلٌ عن الأداء).
  - **الجلسات:** `sessions` (UTC + منع تعارضٍ على مستوى DB) · `session_reports` (مسوّدةٌ بالذكاء → تحرير → اعتماد → تظهر للعائلة).
  - **خدمة توليدٍ واحدة** (`lib/generation/service.ts`، Claude) للعناصر الأصلية ومسوّدات التقارير.
- ✅ **مساحات العمل (Next.js، محميّةٌ بـ proxy):** `/studio` (المعلّم: توليد/اعتماد/إسناد/جدولة/تقارير + تقدّم الطلاب + **«تفرّغي»**: تحدّد مواعيدها بنفسها) · `/learn` (الطالب) · `/guardian` (وليّ الأمر) · `/signup` · دخولٌ موجَّهٌ حسب الدور.
- ✅ **حساب الإدارة/التشغيل `/admin` — مكتملٌ ومفصولٌ عن المعلّم** (دور `admin` منفصل، `assertAdmin()` في رأس كلّ إجراءٍ خدميّ، قشرة `AdminShell` موبايل-أوّلاً). يدير القمع كاملاً:
  - **التسجيل** (`/admin/registrations`): نموذج التسجيل ثلاثيّ المراحل (طالب→وليّ أمر→حجز) → قائمة+تفاصيل مع **شريط مراحل** (التسجيل/الحجز/الاختبار/الجلسة/الدفع/الحسابات) → حجز/إعادة جدولة → اختبار تحديدٍ بالذكاء → **تقرير جلسةٍ تعريفية بالذكاء** (مسوّدة→مراجعة→إرسال للعائلة عبر Resend) → إنشاء حسابات وليّ الأمر/الطالب (**رابط دعوة** لا كلمة مرور) + إسناد المعلّم. مع تعديل بيانات التواصل، حذف/أرشفة، ملاحظةٌ تشغيلية، وسجلّ نشاط.
  - **مرحلة التعلّم** (`/admin/students`): اشتراكات + **فواتير شهرية** (توليد/تحصيل) · انتقالات (إيقاف/استئناف/إلغاء) · **تقييمٌ دوريّ** للعائلة (المعلّم+المنصّة) · ملخّص حضور · **الحالات/الشكاوى** (`/admin/requests`) · لوحةٌ بمؤشّرات (طلاب نشطون/MRR/فواتير متأخّرة) وقائمة **في خطر**.
  - **المعلّمون** (`/admin/teachers`): قائمة + صفحة معلّمٍ (ملفّ/أداء/طلاب مُسنَدون) + **عرضٌ للقراءة لجدول المعلّمة على شكل تقويمٍ بمساحاتٍ لونية** (مخصّص/محجوز/متبقٍّ). جاهزٌ لتعدّد المعلّمين.
- ✅ **المواعيد مملوكةٌ للمعلّمة:** محرّك تكرارٍ أسبوعيّ + استثناءات + استراحة، يُوَلّد الشرائح بـ UPSERT. **المعلّمة تحدّد/تعدّل تفرّغها من حسابها** (RLS تعزل كلّ معلّمٍ لمواعيده)، **والإدارة تشاهد فقط**.
- ✅ **حساب المعلّم — مساحة التعليم (`/studio`):** الرحلة التعليمية القياسية مبنيّةٌ كاملةً:
  - **الرئيسية «مركز قيادة اليوم»**: شريط مؤشّرات + بطاقة «الآن/التالي» (انضمامٌ بنقرةٍ حين يحين الموعد) + خطّ جلسات اليوم + **صندوق «مهامّك اليوم»** يجمع كلّ المعلّق (تقاريرُ ناقصة، واجباتٌ للتصحيح، مسوّداتٌ للاعتماد) بروابطَ تقفز للتبويب الصحيح + قائمة طلابٍ تُبرِز «يحتاج انتباهك».
  - **صفحة الطالب — سبعة تبويبات:** عام · **المنهج** (مصادر تعلّمٍ مرفوعة) · **الخطّة** (محرّرٌ مرئيّ: وحدات→دروس، تحرير/ترتيب/حفظ، قابلٌ للتعديل بعد الاعتماد، ومزامنةٌ idempotent للأهداف؛ يُولَّد من فهرس كتابٍ مرفوع أو يدوياً) · **الجلسات** (متكرّرةٌ ضمن التفرّغ، فيديو داخليّ Daily، تقريرٌ من نموذجٍ بسيطٍ → توليد → اعتماد → مشاركة واتساب) · **الواجبات** (يدويّ من صورة الكتاب + رقميّ مولَّدٌ يُعتمَد ويُسنَد لجلسة) · **الاختبارات** (توليد اختبار وحدةٍ من الأهداف → مراجعة/اعتماد → أداء الطالب → تصحيحٌ آليّ بنتيجةٍ لكلّ مهارة → واتساب) · **التقدّم** (خريطة التفتّح + التحدّث يقيّمه المعلّم).
- ✅ **الفيديو الداخليّ:** Daily.co (`lib/video.ts` + `VideoCall`): غرفةٌ خاصّةٌ لكلّ جلسة، رمزُ انضمامٍ مالكُه المعلّم، تفويضٌ بالدور.
- ✅ **بذرة الديمو (بلا ذكاء/توكِنز):** `scripts/seed-demo-class.mjs` يبني صفّاً من ٥ طلابٍ بشخصياتٍ متنوّعة وأشهرٍ من التاريخ المكتوب يدوياً (خطط/تقدّم/جلسات/تقارير/واجبات/اختبارات) لمعاينة الميزات.
- ✅ **أدوات + بذرة:** `npm run db:migrate` / `db:status` / `db:seed`. حسابات تجريبية: `admin@ward.local` (الإدارة) · `teacher@ward.local` · `student@ward.local` · `parent@ward.local`. **معلّمة حقيقية:** `ward@ward.academy` (غنوة ورد / Ghinwa Ward).

## ٣) القرارات المثبّتة (لا يُعاد فتحها بلا سبب)
- الهوية **مرتكزةٌ على وليّ الأمر** (حسابٌ واحدٌ لوليّ الأمر، أبناءٌ متعدّدون، موافقةٌ شرطٌ لكل حساب).
- **المعلّم يعتمد كلّ مخرَج ذكاء** قبل وصوله للطفل (نموذج الثقة الأساسيّ).
- **التقدّم (النموذج الجديد):** تقييمٌ على مستوى الهدف بأربع حالات (بذرة/برعم/بالون/وردة) يحسمه المعلّم · **decaying average ٦٥/٣٥** على مستوى الهدف · تجميعٌ آليٌّ بسيطٌ للوحدة (**وردة الوحدة**) وللوردة الكبرى (المهارات عبر الوحدات المبدوءة). النطاق A1→B1 على «منهاج وَرد». **مصدر الحقيقة: `Ward_Curriculum_Master_Reference.md`.** *(النموذج الجديد مُعتمَدٌ بالكامل — المعلّم + auto عبر الكتالوج؛ النموذج القديم مُتقاعِد. اكتملت المهاجرة ٢٠٢٦-٠٦-٢٤.)*
- **بيانات أوروبا + عزل RLS** بين الطلاب.
- **توليدٌ أصليّ** للمحتوى (لا ابتلاع كتبٍ محميّة).
- **الفئة ٩–١٣**، عربيٌّ للأهل/المعلّم وإنجليزيٌّ لسطح الطفل.
- **في الواجهة للأهل:** الذكاء الاصطناعي «داعمٌ للمعلّم والطالب» لا أكثر؛ تفاصيله التقنية تبقى في واجهة المعلّم. أرقام غربية، بلا إيموجي، أسماء أولى فقط.
- **فصلٌ صارمٌ للمسؤوليات:** الإدارة (`/admin`) تملك قمع ما قبل التعلّم + الفوترة + الحالات + متابعة المعلّمين؛ المعلّم (`/studio`) للتعليم وتحديد تفرّغه فقط؛ لا تتداخل الأدوار.
- **المعلّمة تملك مواعيدها:** هي تحدّد/تعدّل تفرّغها من حسابها، والإدارة تشاهد جدولها للقراءة فقط.
- **موبايل أوّلاً في كامل رحلة البناء:** كلّ شاشةٍ تُصمَّم وتُتحقَّق على الجوّال أولاً (القشرات تستخدم شريطاً سفلياً على الهاتف).
- **سياسة اللغة (English-first · i18n · قرار 2026-06-24):** كلّ الأسطح إنجليزيّ/LTR افتراضاً (الطفل خالصٌ بلا تبديل)؛ العربيةُ طبقةٌ اختياريةٌ لأسطح الأهل (الهبوط · التسجيل · لوحة وليّ الأمر · التقارير/التواصل) عبر مبدّل + ترجمةٍ بنقرة للتقارير. **i18n من اليوم الأول** (`en.json` أولاً · `ar.json` يُضاف بلا لمسِ كود). الواجهاتُ مبنيّةٌ عربياً الآن → تُحوَّل تدريجياً، لا إعادة بناء. (التفاصيل: `Foundation §13`.)
- **ألوانٌ دلالية لا بنفسجيّ فقط:** البنفسجيّ هو البطل، لكن استخدم الأخضر (نجاح/إتمام) والبطّيخيّ (دعوةٌ دافئة/الخطوة التالية) والورديّ (أخطاءٌ لطيفة/حذف، محدودٌ بلا أحمرَ قاسٍ).
- **سير عمل التصميم (قرارٌ مثبَّت):** Claude Design للهوية البصرية القابلة لإعادة الاستخدام فقط (لوغو · ألوان · خط · أيقونات · الوردة/الشرارة) — **لا شاشات**. الشاشاتُ والصفحاتُ تُبنى في Claude Code مباشرةً. **الكودُ المنشورُ مصدرُ حقيقة الشاشات والهوية بعد تطبيقها؛** ولا يُعاد تمريرُ تعديلات الكود إلى Design (Design أداةُ توليدٍ لا أرشيف). الوردةُ الجديدة (أربع بتلات + بذرة) واللوغو الجديد من Design = الهوية المعتمَدة التي يجب أن يلحق بها الكود.
> التفاصيل الكاملة في: `Ward_Academy_Foundation.md` و`Ward_Academy_Design_System_Brief.md`.

## ٤) الوثائق المرجعية (الحالة النهائية — أربعُ وثائق)
يبدأ كلٌّ منها بلوحة تعاملٍ تشرح حالته وملكيّته:
- 🟢 **`START_HERE.md`** (هذا الملفّ) — النبض والفهرس · حيّ.
- 🧊 **`Ward_Academy_Foundation.md`** — الدستور (حوكمة/أدوار/معمارية/وصلات/أمان/مكدّس/معايير قبول) · مجمَّد. *(دمجُ PRD + Build Brief.)*
- 🧊 **`Ward_Curriculum_Master_Reference.md`** — المنهج والتقييم والتقدّم (A1→B1 · ٢١٥ هدفاً) · مجمَّد.
- 🟢 **`Ward_Academy_Design_System_Brief.md`** — الهوية والألوان والتقدّم البصريّ كما بُنيت فعلاً (استُخرِج من الكود 2026-06-23: أربع بتلات + VocabCounter + أربع حالات + CEFR) · حيّ، بلوحة تعامل.
- `.claude/agents/` — وكلاء المراجعة: `security-reviewer`, `code-reviewer`, `frontend-design-expert`, `scope-guardian`.

> **أُزيلت/أُرشِفت:** `PRD_v1` و`v1_Build_Brief` (اندمجا في Foundation) · `BloomMap_Design_Brief` (متجاوَزٌ بالنموذج الجديد) · `ClaudeCode_Prompts` (انتهى البناء) · ملفّا مراجعة غنوة (أُرشِفا بعد التطبيق).

## ٥) الأدوار (حتى لا يتداخلوا)
- **Claude (الدردشة)** = الاستراتيجية، القرارات، المراجعة. (يحتفظ بذاكرةٍ عبر المحادثات — مرجع الاستمرارية.)
- **Claude Code** = البناء الفعليّ + **صيانة هذا الملفّ**.
- **Claude Design** = التصميم البصريّ على دفعات؛ مخرجاته تذهب للمستودع عبر الـ handoff.

## ٦) طريقة العمل (طقس المزامنة)
- **مصدرُ حقيقةٍ واحدٌ لكلّ مجال:** المنهج → Curriculum Reference · الدستور → Foundation · التصميم → Design Brief · النبض/الحالة → هذا الملفّ. لا تُكرَّر الحقيقةُ في ملفّين.
- **بداية أيّ جلسة** (Code/Design): «اقرأ `START_HERE.md` أولاً قبل أن تبدأ».
- **نهاية أيّ جلسة Code**: «حدّث `START_HERE.md`: الحالة + سجلّ الجلسة + أيّ قرارٍ جديد».
- **القرارُ لا يُعتمَد حتى يُكتَب في مصدر حقيقة مجاله**، ثمّ يُشار إليه هنا. الاتّجاه: قرارٌ → وثيقة المجال → نبضٌ هنا (لا العكس صامتاً).
- **عند العودة إلى Claude (الدردشة)**: شارِك آخر نسخةٍ من هذا الملفّ.

## ٧) خارج النطاق صراحةً (مضادّ التوسّع — راجِع `scope-guardian`)
- لا تطبيقات جوّال أصلية (الويب فقط في v1).
- لا دفع/اشتراكات حيّة (الأسعار عرضٌ فقط الآن).
- لا دردشة/مراسلة فورية داخل المنصّة في v1.
- لا توليد ذكاء يصل للطفل **دون** بوابة اعتماد المعلّم.
- لا جمع بيانات أطفال زائدة عن الحاجة؛ الموافقة شرطٌ دائم.

## ٨) الحقائق التقنية المثبّتة
- **الإطار:** Next.js 16 (App Router) + React 19 + TypeScript + Tailwind v4 + Turbopack.
- **Node 20 إلزاميّ** (Node 24 غير متوافق مع edge-runtime في Next 16) — مثبّت في `.nvmrc`.
- **النشر:** Vercel على `ward.academy`. **المستودع:** github.com/diyaaldinhafez/wardacademy، الفرع `main`.
- **اللغة:** `LanguageProvider` (AR افتراضي/RTL ↔ EN) للتسويق؛ مساحات العمل إنجليزيةٌ/LTR.
- **القاعدة:** Supabase (PostgreSQL، EU/Frankfurt) + RLS. **٥٤ هجرة** (`supabase/migrations/*.sql`) تُطبَّق بـ `npm run db:migrate` (سكربت pg، سلسلة Session pooler في `.env.local` كـ `SUPABASE_DB_URL`). أحدثها: الخطّة بوحداتٍ/دروسٍ معرّفة + ربط الأهداف بالخطّة (0050–0053)، واختباراتُ الوحدة (`assessment_questions` + نتيجةٌ لكلّ مهارة، 0054).
- **حُرّاس الأدوار (server):** `lib/auth.ts` → `assertAdmin()` / `assertInstructor()` في رأس كلّ إجراءٍ يستخدم service-role (RLS وحده لا يكفي إذ يتجاوزه المفتاح الخدميّ). عميل service-role في `lib/supabase/admin.ts`.
- **البريد:** Resend (`lib/email.ts`، نطاق `ward.academy` موثَّق DKIM/SPF) — تأكيد الحجز + رابط الدعوة + تقرير الجلسة التعريفية؛ يتعطّل بلا ضررٍ إن غاب المفتاح. **التواريخ/المناطق الزمنية:** `luxon` + `tenants.timezone`.
- **عملاء Supabase:** `lib/supabase/{client,server,admin}.ts` + `proxy.ts`/`lib/supabase/session.ts` (تحديث الجلسة وحماية `/studio` `/learn` `/guardian`).
- **الذكاء:** `lib/generation/service.ts` (server-only) عبر `@anthropic-ai/sdk` (نموذج `claude-sonnet-4-6`): عناصر/أسئلة، خطّةٌ من المستوى أو من فهرس كتابٍ مرفوع (صورة/PDF/نصّ، استخراجٌ بلا تأليف)، تقريرُ جلسةٍ من مدخلات المعلّم، اختبارُ وحدةٍ من الأهداف، تشخيصٌ داخليّ. سقف إنفاق Anthropic مضبوط ($5 تجريبي).
- **التحقّق:** `npm run build` قبل أي رفع. `.env.local` خارج Git (Supabase + Anthropic + DB URL).

## ✅ مهاجرةُ نموذج التقييم — مُغلَقةٌ ومكتملة (٢٠٢٦-٠٦-٢٤)
> **بند الانحراف أُغلِق.** اكتملت مهاجرةُ نموذج التقييم والتقدّم بالكامل وفق `Ward_Migration_Plan.md` — خمسُ مراحل، خمسُ بوّاباتٍ خضراء:
> - **الكتالوج** (0055): `curriculum_units`/`curriculum_objectives` — «منهاج وَرد» ٣٠/٢١٥ (A1→B1)، كتالوجٌ مشترَكٌ مجمَّد.
> - **نموذج التقييم** (0056): `objective_assessments` (سجلٌّ تراكميّ) + `objective_progress` (حالة) + **decaying average ٦٥/٣٥** بمُشغِّل، وأربع حالات `ward_stage_for_value` (٠/٤/٧/١٠).
> - **التجميع** (`lib/progress/bloom.ts`): وردة الوحدة = متوسّط كلّ أهدافها (غير المُقيَّم=٠) · بتلة المهارة = متوسّط عبر **الوحدات المبدوءة فقط**. دالّةٌ **واحدة** يستهلكها الطالب/وليّ الأمر/المعلّم (اتّساقٌ بالبناء).
> - **مصدر الدليل:** المعلّم يحسم الحالة لكلّ هدف (teacher/mixed)؛ و**auto** = اختبارُ الوحدة مربوطٌ بـ`curriculum_unit_id` (0057) → نسبةُ كلّ مهارة → مفتاح النسبة → `objective_assessments(auto)`.
> - **سقف B1** في كلّ الكود/الـschema · **حُذف الجسر القديم** بالكامل · **تُقوعِد النصف القديم** (0058): `progress_records` + مُشغِّل 0007 + صفوف `objectives` الديمو/Grade6/null. (نسخةٌ احتياطية: `backups/phase5_old_model_2026-06-24.json`؛ `study_plans` وجدول `objectives` schema سليمان — البنّاءُ يُعيد التوليد.)
>
> **مصدرُ حقيقة النموذج:** `Ward_Curriculum_Master_Reference.md`. **متابعاتٌ معروفة (ليست انحرافاً):** «الأساس اللغوي» (VocabCounter) مسارٌ منفصل بلا أهدافٍ كتالوجيّة بعد · دقّةُ auto **لكلّ مهارة** (دقّةُ الهدف بندٌ مستقبليّ) · مُنتقي منهجٍ للمعلّم لبدء أهداف وحدةٍ غير مبدوءة · `descriptor_en` للإنجليزية.

## ٩) الخطوة التالية
**الأساس + الأدوار الأربعة + حساب الإدارة الكامل + حساب المعلّم (مساحة التعليم بالكامل: الخطّة/الجلسات/الفيديو/التقارير/الواجبات/الاختبارات/الرئيسية) + انعكاسها على الطالب ووليّ الأمر: مكتملة.**

**مهاجرةُ نموذج التقييم: مكتملةٌ ومُغلَقة** (انظر القسم أعلاه). **التالي (الأولوية):** متابعاتُ المهاجرة المعروفة — مسارُ «الأساس اللغوي» (مفردات) في الكتالوج · دقّةُ auto على مستوى الهدف · مُنتقي منهجٍ للمعلّم لبدء وحدةٍ غير مبدوءة · `descriptor_en`. **ثمّ:** أسئلةُ **كتابةٍ وتحدّثٍ** في الاختبارات يصحّحها المعلّم (إضافةً للموضوعيّ المُصحَّح آلياً).
**ومراجعةٌ عرضية:** اتّساق موبايل-أوّلاً + الألوان الدلالية عبر مساحة المعلّم والعائلة.

**خارج نطاق v1 (لاحقاً):** بوّابة دفعٍ حقيقية · تقييمات/طلبات يقدّمها وليّ الأمر من حسابه · متابعة مستحقّات المعلّم · فلترة مواعيدٍ لتعدّد المعلّمين · واجهة تعليم حضورٍ للمعلّم.

## ١٠) سجلّ الجلسات
- **2026-06-25 (Code — تدويل · السطح ٦ب: واتساب بلغة الأب — يكتمل السطح ٦ والتدويل):** كلّ قوالب واتساب الموجَّهة للأب تُعرض بلغته (`comms.whatsapp`، احتياط `ar`)، عبر `getTranslations({locale: guardian_locale})` متجاوِزةً locale واجهة المعلّم/الإدارة المفروض. **studio** (`students/[id]`): `reportWaLink`+`assessmentWaLink` → مترجِمان بلغة الأب (`twa`/`tcwa`/`waSkillLabel`)؛ أُضيف `guardian_locale` لاستعلام الـlead. **admin** (`registrations/[id]`): `waTest`/`waBook`/`payment` → `twa` (الـlead فيه `guardian_locale` عبر `select(*)`). **دعوة ProvisionPanel:** تُبنى **خادمياً** في `provisionAccounts` بلغة الأب وتُعاد كـ`inviteMessage` (لا `useTranslations` عميل). قرار الإيموجي: (أ) الوظيفيّ مقبول. **بوّابة ٦ب خضراء:** comms 27/27 · صفر عربيٍّ مكتوب في الملفّات الأربعة · **صفر وسم `PARENT-FACING surface 6` متبقٍّ** · tsc+build خضراء. **التدويل (الأسطح ١–٦) مكتمل.** خارج النطاق المُعرَّف (لقطةٌ للمراجعة): `app/set-password` · `app/video-actions`+`VideoCall` · افتراضات `Bloom.tsx` — عربيةٌ مكتوبةٌ لم تكن ضمن الأسطح الستّة.
- **2026-06-25 (Code — تدويل · السطح ٦أ: البريد بلغة الأب):** القرار (ج): كلّ قالبٍ يصل الأب بلغته المحفوظة، باحتياطٍ عربيّ. **التخزين (الأصغر):** عمودٌ واحد `leads.guardian_locale` (هجرة `0059`، nullable غير كاسر) يُلتقَط في `submitLead` من كوكي `LOCALE` (اللغة التي ملأ بها `/enroll`). **الآليّة الجوهرية:** `lib/email.ts` يُعاد بناؤه ليأخذ `locale` ويعرض عبر `getTranslations({locale: parentLocale, namespace:"comms"})` — **يتجاوز locale واجهة المعلّم/الإدارة المفروض**؛ `layout()` `dir/lang` + التاريخ `setLocale` بلغة الأب. القوالب الثلاثة (`sendBookingConfirmation`·`sendIntroReport`·`sendAccountInvite`) ثنائية؛ **الحدّ:** نصّ التقرير المؤلَّف (`body`) متروك. المستدعون يمرّرون `lead.guardian_locale`: `claimAndConfirm` + `provisionAccounts` + `sendIntroReportAction` (أُضيف `guardian_locale` لـ selects). namespace `comms` (18 مفتاحاً متطابقاً). **بوّابة ٦أ خضراء:** الهجرة طُبّقت · comms 18/18 · صفر عربيٍّ في `lib/email` · tsc+build خضراء. **التالي ٦ب:** واتساب (studio ٢ + admin ٣ + دعوة ProvisionPanel خادمياً).
- **2026-06-25 (Code — تدويل · السطح ٥د: تقاعد النظام القديم — يكتمل السطح ٥):** قطعٌ لا إضافة، صِل-قبل-القطع لكلّ عنصر. **سلسلة التبعيّة المُثبَتة:** `LangToggle`(صفر مستوردين)→`LanguageProvider`(مستورِدُه الوحيد `app/layout.tsx`+`LangToggle`)→`lib/i18n`(مستورِدُه الوحيد `LanguageProvider`)→`lib/content`(مستورِدُه الوحيد `lib/i18n`؛ بنك أسئلته يغذّي القاموس الميّت فقط — الاختبار الحيّ يقرأ من DB). الخطوة ١ (صِل): إزالة `LanguageProvider` من `app/layout.tsx` + جعل `html` `lang`/`dir` **مقوداً بالـlocale** (إنجليزيّ-أوّلاً→ltr). ثمّ الحذف بترتيبٍ عكسيّ (git rm صريح): `LangToggle`·`LanguageProvider`·`lib/i18n`·`lib/content`. وحذف `auth.*`/`enroll.*` المنقولَين الميّتَين من `messages/*` (صفر مستهلكين، sed على النطاق فقط). **بوّابة ٥د خضراء:** صفر بقايا (`useT`/`useLang`/`LanguageProvider`/`lib/i18n`/`lib/content` = صفر في الشيفرة) · تطابق ثنائيّ (common14·landing206·enrollForm62·placement15·guardian28) · لا namespace يتيمٌ في ar · tsc+build خضراء (٢٢ مساراً). **السطح ٥ مكتمل — نظامٌ واحدٌ: next-intl + كوكي LOCALE.** بقي السطح ٦ (التواصل).
- **2026-06-25 (Code — تدويل · السطح ٥ج-٢: الاختبار + الحجز بالرمز — يكتمل السطح ٥ج):** `app/t/[token]`+`LeadTestForm` (الأب يفتح، الطفل يحلّ) → namespace `placement` (15 مفتاحاً) + حاوية `dir` مقودة + مبدّل؛ أخطاء `app/t/actions.ts` ثنائيةٌ عبر `tErr`. **أسئلة الاختبار محتوًى تعليميّ متروك** (من DB، تُعرض `dir="ltr"`). اكتشافٌ: `app/book/[token]` (يعرض `BookFlow` المُدوَّل في ٥ب) كان نصفَ ثنائيّ (إطارُه عربيٌّ بلا dir) → أُكمل (4 مفاتيح في `enrollForm`: bookMetaTitle/alreadyBooked*/childFallback + dir+مبدّل). تهذيب metadata صفحات redirect: `/signup`→`enrollForm.metaTitle`، `/placement`→`placement.metaTitle` (كانت `/signup` عربيةً مفردةً شاذّة). **بوّابة ٥ج-٢ خضراء:** placement 15/15 · enrollForm 62/62 · صفر عربيٍّ في الشريحة · tsc+build خضراء. **السطح ٥ج مكتمل.** بقي **5د** (تقاعد القديم) لإنهاء السطح ٥.
- **2026-06-25 (Code — تدويل · السطح ٥ج-١: الهبوط الذرّيّ):** تحويلٌ ذرّيٌّ لكامل رسم الهبوط (`Landing` + 7 فرعية: Outcomes·StickyTrialBar·SessionScene·ProductShowcase·BloomReport·TeacherDashboard) من النظام القديم إلى next-intl: `useT().landing`→مساعدٌ مطبوع `useLandingMessages()` (يحفظ وصول `L.x.y` كما هو، ويُبقي callbacks الـ`.map` مطبوعةً بلا `any`)، `LangToggle`→`LocaleSwitcher`، حاوية `dir` مقودة بالـlocale في `Landing`. الهبوط **مبنيٌّ منطقياً أصلاً** (`ms`/`ps`/`-start`/`end`/`text-start`/`pe`)؛ `left-1/2` الوحيد توسيطٌ محايدُ الاتّجاه (لا يُلمَس). **إصلاح عيبين (لا نقل):** `pricing.note` يورو→**USD** (اللغتان؛ الباقات بالدولار)، `favourite`→`favorite` (أمريكية). **توحيد المخاطبة العربية على المفرد الودود** (3: `final.title` ابدؤوا→ابدأ · `final.sub` تعرّفوا/شاهدوا→تعرّف/شاهد · `how.steps.2.d` تصلكم→تصلك). **بوّابة ٥ج-١ خضراء:** صفر `useT` في الرسم · صفر عربيٍّ مكتوب · tsc+build خضراء. **مؤجَّل ٥ج-٢:** `app/t/[token]`+`LeadTestForm`. **ميّتٌ للحذف في 5د:** `auth.*`/`enroll.*` المنقولان + `LangToggle` (يتيمٌ الآن).
- **2026-06-25 (Code — تدويل · السطح ٥ب: رحلة التسجيل):** اكتشافٌ مهمّ: نموذج `/enroll` الحيّ (`EnrollFlow`) **لا يستعمل قاموس `lib/i18n` المنقول** (enroll.* المنقول لمسارٍ آخر — `/login`·`/signup`·`/placement`·`/t` تستهلكه)؛ له نصوصُه الخاصّة. ⇒ namespace **`enrollForm`** جديد (58 مفتاحاً متطابقاً، إنجليزيةٌ أمريكيةٌ دافئةٌ مؤلَّفةٌ حديثاً + عربيةٌ محسّنة). `page` → حاوية `dir` مقودة + `LocaleSwitcher` + `generateMetadata`. `EnrollFlow/BookFlow/SlotPicker` → `useTranslations`/`useLocale`، تواريخ بالـlocale، تسميات الخيارات locale-aware عبر `labelOfEn` (ع=`Opt.label`)، المهارات من `common.skills` (ثنائية). `actions.ts` → أخطاء الأب ثنائيةٌ عبر `enErr` (locale-driven). أُضيفت `COUNTRIES_EN`/`NATIONALITIES_EN`/`HOME_LANGUAGES_EN` لـ`enrollOptions` (القوائم العربية كانت ستظهر على الفورم الإنجليزيّ). **صفر صنفٍ فيزيائيّ** في الثلاثة. **بوّابة ٥ب خضراء:** enrollForm 58/58 · القوائم EN↔AR متطابقةُ الطول (174/174 · 173/173) · صفر عربيٍّ في الواجهة (يبقى فاصل `join("، ")` تخزينيّاً) · tsc+build خضراء. **مؤجَّل (٥ج/تابع):** `app/t/[token]` (يعرض BookFlow) يحتاج حاوية `dir`+مبدّل.
- **2026-06-24 (Code — تدويل · السطح ٥أ: نقل القاموس — بلا تغيير سلوك):** نقلُ القاموس الثنائيّ القديم `lib/i18n.ts`/`lib/content.ts` (٤٦٢ سطراً) إلى `messages/en.json`+`ar.json` تحت `landing.*` (206) · `auth.*` (login/signup/starter/ui — 32) · `enroll.*` (137). **النظام القديم باقٍ نشطاً** (`LanguageProvider`/`useT`/`LangToggle`) — لا مكوّنَ مُسّ. الدالّتان (`greeting`/`questionOf`) → ICU (`greetingNamed`/`greetingAnon`/`questionOf {i}/{n}`). **`enroll.test.questions` لم يُنقَل** (محتوًى تعليميّ يبقى بياناتٍ في `content.ts`). أُزيلت بقايا `landing:{}`/`enroll:{}` الفارغة (تكرارٌ كان يُفرِغ المفاتيح). **بوّابة ٥أ خضراء:** تطابق en/ar صفر فرق في الأسطر الخمسة (landing 206 · auth 32 · enroll 137 · guardian 28 · common 14) · tsc+build خضراء.
- **2026-06-24 (Code — تدويل · السطح ٤: وليّ الأمر — أوّل سطحٍ ثنائيّ):** النموذج الموروث للأسطح ٥·٦. **مقودٌ بالكوكي لا مفروضٌ إنجليزيّاً:** `guardian/layout.tsx` → `dir` ديناميكيّ عبر `getLocale()` (حاوية واحدة، en→LTR · ع→RTL). `LocaleSwitcher` المبنيّ سابقاً يُوظَّف في رأس `WorkspaceHeader` عبر فتحة `rightSlot` جديدة. `messages/en.json`+`ar.json`: namespace `guardian` (٢٨ مفتاحاً متطابقاً) + إكمال `common` (`actions.logout`/`skills`/`vocab`). **القرار (أ):** تسميات المهارات من `common.skills` ثنائية (تنقلب مع اللغة). الحدّ: نصّ تقرير المعلّم + `title_ar` + اسم الطفل = بياناتٌ لا تُترجَم. `pr-4`→`pe-4` (الوحيد الفيزيائيّ). **بوّابة ٤ خضراء:** تطابق مفاتيح en/ar صفر فرق (guardian 28/28 · common 14/14) · صفر عربيٍّ مكتوبٍ في الكود · tsc+build خضراء.
- **2026-06-24 (Code — تدويل · السطح ٣د: رسائل النظام — يكتمل `/admin`):** كلّ رسائل `app/admin/actions.ts` (~٢٩ خطأً عبر مساعد `adminErr`→`admin.errors.*`) + تسميات سجلّ النشاط (`logEvent`) إنجليزيّةً + مُدخَل عمر الطالب لتوليد الاختبار (`Age N`). كلّها **داخليّة** (قوالب الأهل في `lib/email`، للسطح ٦). **بوّابة ٣د خضراء — `/admin` كاملاً صفر عربيّ داخليّ** · المفاتيح تُحَلّ · tsc+build خضراء. **السطح ٣ (الإدارة) مكتملٌ بالكامل.** المؤشَّر للأب الباقي (للسطح ٦): ٣ رسائل واتساب في `registrations/[id]` + دعوة `ProvisionPanel`.
- **2026-06-24 (Code — تدويل · السطح ٣ج: الطلاب + المعلّمون):** `students/page` (الاشتراكات) + `students/[id]` (الخطّة · الفواتير · الحضور · تقييم العائلة · الطلبات/الشكاوى) + `teachers/page` + `teachers/[id]` (الأداء · الطلاب المُسنَدون · الملفّ · الجدول للعرض) → `admin.students/teachers`. ثوابت إنجليزية: `REQUEST_TYPE_EN`/`REQUEST_STATUS_EN` (lib/requests) + `enrLabel` محليّ. كلّها **داخليّة** (تقييم العائلة يسجّله المشغّل، لا يُرسَل). **بوّابة ٣ج خضراء:** صفر عربيٍّ في الأربعة · المفاتيح تُحَلّ · tsc+build خضراء. **المتبقّي في `/admin` = ٣د فقط** (`actions.ts`) + المؤشَّر للأب في `registrations/[id]` (٣ رسائل واتساب، للسطح ٦).
- **2026-06-24 (Code — تدويل · السطح ٣ب: قمع التسجيل):** أكبر شريحة إدارة. `registrations/[id]` (التفاصيل · الحجز · اختبار التحديد · تقرير الجلسة التعريفية · الدفع · التجهيز · الأرشفة) + `registrations/page` (نوافذ الجلسات التعريفية · التبويبات · البحث · القائمة) + `PipelineStepper` (async) + `DeleteLeadButton` + `ProvisionPanel` → `admin.registrations/pipeline/provision/deleteLead`. ثوابت إنجليزية للمكتبات: `LABELS_EN`+`labelOfEn` (enrollOptions) · `INTRO_LABELS_EN` (introReport) · `LEAD_STATUS_EN`+`PIPELINE_EN`+`ACTION_LABEL_EN` (leads، و`actionFor` صار يُرجع key لا نصّاً). **🔖 نصوصٌ موجَّهةٌ للأب أُشِّرت وتُرِكت عربيّةً (للسطح ٦):** ٣ رسائل واتساب (رابط الاختبار/الحجز/الدفع) في `registrations/[id]` + رسالة دعوة التجهيز في `ProvisionPanel` — الأهل عربيّ، تُجعَل ثنائيةً في سطح التواصل. **بوّابة ٣ب خضراء:** صفر عربيٍّ في واجهة الإدارة الداخلية (الباقي حصراً = المؤشَّر للأب) · المفاتيح تُحَلّ · tsc+build خضراء. **المتبقّي:** ٣ج (students/* + teachers/*) · ٣د (actions.ts).
- **2026-06-24 (Code — تدويل · السطح ٣أ: إطار الإدارة + اللوحة):** تجزئةٌ رباعيّة لـ`/admin` (٣أ الإطار+اللوحة · ٣ب قمع التسجيل +ProvisionPanel · ٣ج الطلاب+المعلّمون · ٣د رسائل actions). **٣أ مُنجَزة:** `AdminShell` (تنقّل/هوية/خروج، LTR) + layout (+مزوّد en مفروض) + لوحة المؤشّرات (`page.tsx`) + الحالات (`requests`) + `AvailabilityView` → مساحة `admin.*` بنبرةٍ وظيفيّة. أُضيفت ثوابت إنجليزية للمكتبات المشترَكة: `PIPELINE_LABEL_EN` (lib/leads) · `REQUEST_TYPE_EN`/`REQUEST_STATUS_EN` (lib/requests). **بوّابة ٣أ خضراء:** صفر عربيٍّ في الشريحة (٥ ملفّات) · المفاتيح تُحَلّ · tsc+build خضراء. **المتبقّي:** ٣ب (registrations/* + PipelineStepper + DeleteLeadButton + ProvisionPanel) · ٣ج (students/* + teachers/*) · ٣د (actions.ts).
- **2026-06-24 (Code — تدويل · السطح ٢د: النظام والتفرّغ — يكتمل `/studio`):** **رسائل `actions.ts`** (~٢٥ خطأً) عبر مساعد `studioErr`→`studio.errors.*`، و**النصوص المولَّدة المخزَّنة** إلى الإنجليزية (scope_label «Ward Curriculum · Level X» · milestone · «Unit test: X» · افتراضات). `availability-actions` (خطآن). **صفحة «تفرّغي»** + `RuleForm` + `AvailabilityMatrix` المشترَكة (مع الإدارة) → مساحة `availability.*`، وقُلِبت المصفوفة البصرية إلى **LTR** (left بدل right · AM/PM · أيام إنجليزية `WEEKDAY_EN`). `SlotForm` (كودٌ ميّت) نُظّف بحرفيّاتٍ إنجليزية. **بوّابة ٢د خضراء — `/studio` كاملاً صفر عربيّ** (كلّ مساراته + المكوّنات التي يصيّرها، شامل RuleForm/Matrix) · ٢د المفاتيح تُحَلّ · `/studio/login` إنجليزيّ · `/studio` يعمل (307) · build أخضر. **السطح ٢ (المعلّم) مكتملٌ بالكامل.** المتبقّي: `ProvisionPanel` عربيّ لكنّه **إدارةٌ فقط** (يُصيَّر في `/admin` لا `/studio`) → السطح ٣. (RuleForm/AvailabilityMatrix جُهِّزتا مسبقاً للإدارة.)
- **2026-06-24 (Code — تدويل · السطح ٢ج: صفحة الطالب — الجلسات/الواجبات/الاختبارات/التقدّم):** اكتمل **ملفّ صفحة الطالب كاملاً** (page.tsx صفر عربيّ) + `ItemCard` (async `getTranslations`) + `SessionScheduleForm` (`useTranslations`) + `WEEKDAY_EN` في `lib/availability`. كلّ المساعدات المضمّنة (subStatus·HW_KIND·REPORT_FIELDS·SessionCard·HwSessionCard·ManualHwItem·…) دُوِّلت إلى `studio.student.*` بنبرةٍ احترافية. **قراران مطبّقان:** (١) «objective» يبقى مصطلحاً للمعلّم. (٢) مسار «from index» متجاوَزٌ (مرشّحٌ للحذف) — وصياغة ٢ب: «…build a plan from it automatically.». **قالبا واتساب** (تقرير الجلسة/نتيجة الاختبار) دُوِّلت تسمياتُهما إنجليزيّاً (افتراض English-first)، والطبقةُ ثنائيّة اللغة للأهل في **سطح التواصل ٦**. **بوّابة ٢ج خضراء:** صفر عربيٍّ في كلّ ملفّ صفحة الطالب + المكوّنات المشترَكة · المفاتيح تُحَلّ (بما فيها مصفوفات `reportFields`) · build أخضر. **المتبقّي في `/studio` = السطح ٢د فقط** (`actions.ts` رسائل · صفحة «تفرّغي» + `SlotForm`). (`ProvisionPanel` إدارةٌ = سطح ٣.)
- **2026-06-24 (Code — تدويل · لهجة + السطح ٢ب):** **قرارٌ معتمَد: الإنجليزية الأمريكية** عبر المنصّة (Enroll·color·practice·organize·center·traveling). تصحيحٌ رجعيّ: `Enrol your child → Enroll your child` (والالتزام بها في كلّ ما يأتي). **٢ب (صفحة الطالب — عام/المنهج/الخطّة + `PlanBuilder`):** كلّ نصوص هذه التبويبات + رأس الصفحة + تسميات التبويبات السبع + `PlanBuilder` → `studio.student.*`/`studio.planBuilder.*` بنبرةٍ احترافيةٍ بالمصطلح الصحيح (objective · assessment · placement · plan draft/approved · diagnostic). الخادم `getTranslations({locale:"en"})`، و`PlanBuilder` (client) `useTranslations` تحت مزوّد الـworkspace المفروض. **مُتروكٌ صراحةً** (محتوى): أوصاف الدروس · `title_ar` · تأليف الخطّة/التشخيص. **بوّابة ٢ب خضراء:** صفر عربيٍّ في الشريحة (التبويبات الثلاث ٣٩٦–٥٧٢ + الرأس + PlanBuilder) · ٢٤ مفتاحاً تُحَلّ · build أخضر. (تبويبات الجلسات/الواجبات/الاختبارات/التقدّم ومساعداتها المضمّنة لـ٢ج — يبقى فيها عربيٌّ مؤقّت.)
- **2026-06-24 (Code — تدويل · السطح ٢أ: إطار المعلّم `/studio`):** أوّلُ ٤ بوّاباتٍ فرعيّة للمعلّم (٢أ الإطار · ٢ب صفحة الطالب ١ · ٢ج صفحة الطالب ٢ · ٢د النظام/التفرّغ). **٢أ مُنجَزة:** الشِّل (`TeacherShell`: تنقّل/هوية/خروج) · الرئيسية «مركز قيادة اليوم» · قائمة الطلاب · الدخول (`LoginForm`+صفحته) · `WorkspaceHeader` المشترَك — كلّها إلى `messages/en.json` (مساحة `studio.*`) بنبرةٍ احترافيةٍ بالمصطلح الصحيح. **قرارٌ معماريّ:** `NextIntlClientProvider locale="en"` يغلّف layout كلّ سطحٍ مفروضٍ إنجليزيّاً (studio workspace + studio/login + **`/learn`**) فتعمل `useTranslations` بالإنجليزية مهما كان الكوكي؛ والخادم بـ`getTranslations({locale:"en"})`. **أُصلِح تسريبٌ:** `WorkspaceHeader` (مشترَك) صار client+`useTranslations` — كان «تسجيل الخروج» عربيّاً يظهر في `/learn`؛ وكان `app/learn/layout.tsx` يفرض `dir="rtl" lang="ar"` فصُحِّح إلى LTR/en + مزوّد en. **قاعدةٌ معتمَدة:** فحص «صفر عربيّ» يشمل المكوّنات المشترَكة لا مجلّد السطح فقط. **بوّابة ٢أ خضراء:** صفر عربيٍّ في الشريحة (incl. المشترَك) · ٣٢ مفتاحاً تُحَلّ · `/studio/login` يعرض إنجليزيّاً (Sign in·Email·Password·Enrol your child) · `/studio` يعمل (307) · build أخضر. (التبويباتُ السبعة لصفحة الطالب في ٢ب/٢ج.)
- **2026-06-24 (Code — تدويل · السطح ١: الطفل `/learn`):** دُوِّلت **نصوص الواجهة فقط** في `/learn` إلى `messages/en.json` (مساحة `learn.*` + `common.skills/vocab`) بنبرة الطفل المبسّطة. **إنجليزيٌّ خالص:** فُرِض `locale:"en"` في كلّ نداءات `getTranslations` بصفحة `/learn` وأفعالها (لا يتأثّر بكوكي اللغة العامّ)، و`AnswerForm` صار يستقبل النصوص props (لا `useTranslations`)، و`<main lang="en" dir="ltr">`. **المحتوى التعليميّ مُتروكٌ صراحةً** (مُعلَّمٌ بتعليقات): نصوص الأسئلة/الخيارات (من بنك الأسئلة) · **عناوين وحدات الكتالوج `title_ar`** (تبقى عربيّةً حتى يُكتب `title_en`/`descriptor_en`) · عناوين/أوصاف الخطّة وتقارير الجلسات والواجبات (تأليف المعلّم). **بوّابة ١ خضراء:** صفر عربيٍّ في كود `/learn` (page+AnswerForm+actions) · ٧١ مفتاحاً كلّها تُحَلّ في en.json · `/learn` يعمل (307→login لا 500) · tsc+build خضراء. **مراجعةُ النبرة (ضياء):** بُسِّطت صياغاتٌ، و**أُزيل الإيموجي الشائع كليّاً من سطح الطفل** (🎯🌟🎉…) — تبقى رموزٌ وظيفيةٌ فقط (✓ ↓)؛ **بندٌ مستقبليّ:** أيقونات وَرد المخصّصة تحلّ محلّ الإيموجي. اعتُمِد السطح ١.
- **2026-06-24 (Code — تدويل · البنية التحتية):** بدأ تنفيذ `Ward_i18n_Plan.md` (English-first). **المكتبة المختارة: `next-intl` v4** (الأنسب لـ App Router — تعمل في Server + Client Components حيث تعيش الأسطح الحقيقية؛ الدِّكت القديم client-only لا يصلها)، **بلا بادئات مسار** — اللغة في كوكي `LOCALE` (افتراض `en`) عبر `i18n/request.ts` + `createNextIntlPlugin` في `next.config.ts`. **بنية المفاتيح هرمية دلالية** `surface.area.element` في `messages/en.json` (قاعدة + مساحات `learn/studio/guardian/admin/landing/enroll` فارغة تُملأ سطحاً سطحاً)، و`messages/ar.json` يُضاف drop-in بلا كود. `components/LocaleSwitcher.tsx` (كوكي + `router.refresh`) لأسطح الأهل فقط. أُضيف `NextIntlClientProvider` في الجذر **إضافيّاً** (لم تُمَسّ `lang/dir` ولا `LanguageProvider` القديم — يتقاعد سطحاً سطحاً). **بوّابة البنية خضراء:** `/i18n-demo` يعرض المفتاح التجريبيّ من en.json (server+client)، والمبدّل يقلب EN↔ع فيتغيّر النصّ والاتجاه (en/ltr ↔ ar/rtl) بدليل runtime، والبناء أخضر. **الأسطح لم تبدأ بعد** (تنتظر إشارة ضياء — السطح ١: الطفل `/learn`).
- **2026-06-24 (Code — مهاجرة التقييم · المرحلة ٥: إغلاق المهاجرة):** **وصلُ auto بالكتالوج ثمّ تقاعدُ النصف القديم**، بثلاثة حرّاس (صِل‑قبل‑القطع · تطابُق المعرّفات · نسخة‑قبل‑الإسقاط). **الحارس ٢:** بُرهِن أن لا تطابُقَ معرّفات — القديم uuid/ديمو/نصّ‑حرّ، الكتالوج `A1-U01-L1`، و`assessment_questions` بلا `objective_id` (مهارةٌ فقط)؛ فلا remap بل ربطٌ جديد. **0057:** `assessments.curriculum_unit_id`؛ توليدُ الاختبار من وحدة كتالوج؛ و`submitAssessment` يكتب `objective_assessments(evidence='auto')` لكلّ أهداف (الوحدة، المهارة) عبر مفتاح النسبة → المُشغِّل يحرّك القيمة. **تحقّق (الحارس ١):** اختبارٌ آليٌّ حرّك `objective_progress` فعلاً (قراءة→10/وردة) **قبل** أيّ إسقاط. **نسخة (الحارس ٣):** `backups/phase5_old_model_2026-06-24.json` (objectives/progress_records/items/submissions). **0058 (تقاعد):** أُسقِط مُشغِّل 0007 + جدول `progress_records`، ونُظِّفت صفوف `objectives` (٦٧ ديمو/Grade6/null → cascade على items/submissions؛ `study_plans` سليمة عبر set‑null). حُدِّث بنّاءُ الخطّة لإزالة قراءات `progress_records`. **بوّابة ٥ خضراء:** progress_records مُسقَط (`to_regclass=null`) · مُشغِّل 0007 مُسقَط · objectives صفر · study_plans ٥ · الاختبار يحمل `curriculum_unit_id` · auto يحرّك القيمة · build أخضر. **بند الانحراف مُغلَق — المهاجرة مكتملة.**
- **2026-06-24 (Code — مهاجرة التقييم · المراحل ٢→٤):** **المرحلة ٢ (نموذج التقييم):** هجرة 0056 — `objective_assessments` (سجلٌّ تراكميّ) + `objective_progress` (حالةٌ حاليّة) + `ward_stage_for_value()` + مُشغِّلا **المتوسّط المتناقص** `round(0.35·prev+0.65·new,4)` (أوّل=new) + RLS كنمط `progress_records`. بوّابة ٢: 10→6.1→8.635 ✓. **المرحلة ٣ (التجميع والربط):** `lib/progress/bloom.ts` = دالّة تجميعٍ **واحدة** يستهلكها الثلاثة (اتّساقٌ بالبناء): وردة الوحدة = متوسّط كلّ أهدافها (غير المُقيَّم=٠)؛ بتلة المهارة = متوسّط عبر **الوحدات المبدوءة فقط** (غير المبدوءة تُستثنى، لا صفراً). وُصِلت `/learn`·`/guardian`·`/studio`(التقدّم)، وأُضيف **تقييمٌ لكلّ هدفٍ للمعلّم** (`recordObjectiveAssessment`→المُشغِّل). بوّابة ٣: الاستثناء يحوّل 2.5→5.0 ✓. **المرحلة ٤ (سقف B1 + كنس الكود الميّت):** ضُيِّق CEFR إلى A1→B1 في ٨ مواضع (مصفوفتا التحديد · نصوص توليد/تسجيل/i18n · تعليق schema 0004)؛ وحُذف الجسر الميّت (`petalValues`/`valueOf`/`vocabMastered`/`isMastered`/`Petal` + `setSkillAssessment`/`SPEAKING_LEVELS`/`TEACHER_ASSESSED`). **بوّابة ٤ خضراء:** لا مستوى >B1 · لا استدعاءَ حيٍّ للمحذوف · `progress_records`/auto **سليمان لم يُمَسّا** · tsc+build خضراء. ⚠️ **المهاجرة غير مكتملة** — التصحيح الآليّ (auto) للمرحلة ٥ (انظر «حالة المهاجرة» في بند الانحراف).
- **2026-06-24 (Claude الدردشة — قرار اللغة + اعتماد بوّابة المرحلة ١):** اعتُمِد قرارٌ معماريّ **English-first + i18n**: كلّ الأسطح إنجليزيّ/LTR افتراضاً (الطفل خالصٌ بلا تبديل)، والعربيةُ طبقةٌ اختياريةٌ لأسطح الأهل (الهبوط · التسجيل · لوحة وليّ الأمر · التقارير/التواصل) عبر مبدّل + ترجمةٍ بنقرة — تحوّلٌ تدريجيٌّ بلا إعادة بناء (حُدِّث `Foundation §13/§15` و`Design Brief §1`). **بوّابة المرحلة ١ طوبِقت بالمرجع واعتُمِدت** (٢١٥/٣٠ · المهارات · evidence · ≤B1+). الكتالوج عربيٌّ الآن (`descriptor_ar`) مع `descriptor_en` لاحقاً. **أُذِن ببدء المرحلة ٢.** قرارُ اللغة مستقلٌّ عن المهاجرة.
- **2026-06-24 (Code — مهاجرة التقييم · المرحلة ١/٥: الكتالوج):** بدأت مهاجرةُ نموذج التقييم وفق `Ward_Migration_Plan.md`، مرحلةً مرحلةً ببوّاباتٍ بينية. **المرحلة ١ (الكتالوج) مكتملةٌ واجتازت بوّابتها:** هجرة 0055 أنشأت جدولَي الكتالوج المشترَك `curriculum_units` + `curriculum_objectives` (للقراءة لأيّ مُصادَق · الكتابة بالخدمة فقط · غير مرتبطٍ بمستأجِر)، وحُمِّل **«منهاج وَرد» (٣٠ وحدة · ٢١٥ هدفاً)** عبر `scripts/load-ward-catalog.mjs` (بحارس سلامة UTF‑8 يرفض العربيّ التالف). **بوّابة ١ خضراء:** ٣٠/٢١٥ · مهارات {استماع ٦٠·محادثة ٦٧·قراءة ٥٢·كتابة ٣٦} · evidence {auto ١١٢·teacher ٦٧·mixed ٣٦} · CEFR ≤ B1+ · لا وحدةَ فارغة. **قرارٌ موثَّق:** سُمّي الكتالوج `curriculum_*` (لا `units`/`objectives`) تفادياً للتصادم مع جدول `objectives` الحيّ (النموذج القديم، يُتقاعَد في مرحلةٍ لاحقة) — **تغييرٌ إضافيٌّ غير كاسر**. **المتبقّي (المراحل ٢–٥):** `objective_assessments`+`objective_progress` · رياضيّات decaying average ٦٥/٣٥ · تجميع الوحدة/الوردة الكبرى وربطُ المكوّنات · سقف B1 + إزالة الجسر · تحقّقٌ طرفيٌّ وإغلاقُ بند الانحراف.
- **2026-06-23 (Code — تطبيق الهوية البصرية الجديدة):** استُورِد نظام التصميم «Ward Academy Design System» وطُبِّقت **الهوية البصرية فقط** (لا منطق تقييم). **الوردة = أربع بتلات** (المهارات الأربع) و**«الأساس اللغوي» عدّادٌ منفصل (VocabCounter)** لا بتلة؛ **دورة الوحدة أربع حالات** seed→bud→balloon→bloom بقيمةٍ 0–10 (`stageForValue`). أُعيدت كتابة `components/bloom/Bloom.tsx` (FlowerProgress·UnitBloom·VocabCounter·ProgressBar=إتمام·ScopeChip=CEFR·LevelBadge·StreakChip·ObjectivePetals) و`lib/skills.ts` (٤ مهارات + مسار مفردات + `stageForValue`/`valueOf`/`petalValues`/`vocabMastered`)؛ التوكِنز موجودةٌ في `ward.css`، والأصول في `public/ward/*.svg` والأيقونة الجديدة. طُبِّقت على **الشاشات القائمة فقط** (`/learn`·`/guardian`·`/studio`/التقدّم) + علامات زهرة الهبوط (FlowerMark·HeroBloom·WhiteFlower) صارت أربع بتلات. أُزيل النموذج القديم (`components/BloomFlower.tsx`+`components/UnitBloom.tsx`، البتلة الخامسة، فرع المدرسة في ScopeChip). أُخرِج `Ward_Academy_Design_System_Brief.md` كما بُنيت. build نظيف. **يبقى منفصلاً: مهاجرةُ رياضيّات التقييم** (decaying average ٦٥/٣٥ · وردة الوحدة · الوردة الكبرى عبر الوحدات المبدوءة) من `Ward_Curriculum_Master_Reference.md` — `petalValues`/`valueOf` الحاليّان جسرٌ مؤقّت يُغذّي المكوّنات الجديدة.
- **2026-06-23 (Claude الدردشة — مزامنةُ المنهج والتقييم والوثائق):** **تجمّد مجال المنهج**: كتالوج «منهاج وَرد» A1→B1 (٣٠ وحدة · ٢١٥ هدفاً) في `Ward_Curriculum_Master_Reference.md`، بنظام تقييمٍ جديد (decaying average ٦٥/٣٥ · أربع حالات · وردة الوحدة · الوردة الكبرى عبر الوحدات المبدوءة · مفتاح سلوكيّ + مفتاح نسبة)، واعتُمِدت مراجعةُ المعلّمة (غنوة) وطُبِّقت. **توحيدُ الوثائق:** دُمِج PRD + Build Brief في `Ward_Academy_Foundation.md` (مجمَّد)، وأُزيل المُتجاوَز (BloomMap · Prompts). **هذا الملفّ → النسخة ٦:** لوحةُ تعامل، نطاق A1→B1، قرارُ التقدّم الجديد، تنبيهُ انحرافٍ للكود، فهرسُ الأربع وثائق. **لم يُلمَس الكود** — المهاجرةُ مهمّةُ Code قادمة.
- **2026-06-20 (Code — توحيد على «منهاج وَرد» CEFR):** أُلغي مسار **«المنهج المدرسيّ»** نهائياً والتوحيد على منهجٍ خاصٍّ بالمنصّة بمعيار **CEFR (A1→B2)**. **تغييرٌ غير كاسر** للـ schema: أُبقيت أعمدة `track/scope_label/milestone_label` و enum `objective_track` كما هي، وأُوقِف استخدام قيمة `'school'` (كلّ كتابةٍ = `'cefr'`) + **تطبيعٌ آمنٌ للبيانات** (`track school→cefr`). الواجهة/التوليد: إسقاط فرع المدرسة من `generatePlan`/`generatePlanFromIndex` (الفهرس المرفوع صار «كتاب منهجٍ بمستوى CEFR»)، إزالة اختيار المسار وحقول الصفّ/الفصل من نماذج الخطّة، و`ScopeChip` صار CEFR-only في الشاشات الثلاث. **الهبوط/القمع**: إبراز «منهاج وَرد (CEFR)» + الخطّة المخصّصة من التشخيص (استمارة+تحديد+تجريبية+ملاحظات المعلّم) + توليد الذكاء حسب الهدف/الزمن تحت إشراف المعلّم؛ حذف خطاب «الدعم المدرسيّ» وهدفه ومسارَيه، حذف حقل **الصفّ** من الاستمارة، وإعادة تأطير **نوع المدرسة** كـ«بيئة الطفل اللغوية». build نظيف.
- **2026-06-20 (Code — حساب المعلّم: مساحة التعليم):** بُنيت الرحلة التعليمية كاملةً (هجرات 0039–0054، نسخة الوثيقة ٥). **صفحة الطالب** أُعيد تنظيمها لسبعة تبويبات (عام/المنهج/الخطّة/الجلسات/الواجبات/الاختبارات/التقدّم). **محرّر خطّةٍ مرئيّ** (وحدات→دروس، تحرير/ترتيب/حفظ، قابلٌ للتعديل بعد الاعتماد، مزامنةٌ idempotent للأهداف عبر `plan_id`+`plan_lesson_id`؛ يُولَّد من فهرس كتابٍ مرفوع أو يدوياً). **جلسات**: مواعيدُ متكرّرةٌ ضمن التفرّغ، **فيديو داخليّ Daily**، وتقريرٌ من **نموذجٍ بسيطٍ (اختياراتٌ جاهزة + سؤالٌ مفتوح) → توليد → اعتماد → مشاركة واتساب**، وزرُّ الانضمام لأقرب جلسةٍ فقط. **واجبات** في تبويبٍ مستقلّ (يدويّ من صورة الكتاب + رقميّ مولَّد). **اختباراتُ الوحدة (v1)**: تُولَّد من أهداف الوحدة → مراجعة/اعتماد → يؤدّيها الطالب في `/learn` → **تصحيحٌ آليّ بنتيجةٍ لكلّ مهارة** → واتساب. **الرئيسية «مركز قيادة اليوم»** (الآن/التالي + صندوق مهامّ موحّد بروابطَ عميقة + خطّ اليوم + طلابٌ يحتاجون انتباهاً). **عكسٌ على العائلة**: الطالب يرى نتائج اختباراته؛ وليّ الأمر يرى التقارير الكاملة + نتائج الاختبارات + الخطّة بوحداتها. **بذرة ديمو يدويّة** (بلا توكِنز): صفٌّ من ٥ طلابٍ متنوّعين. حذف حساب طالبٍ تجريبيٍّ قديم نهائياً.
- **2026-06-19 (Code — حساب الإدارة):** بُني **حساب الإدارة/التشغيل `/admin`** كاملاً ومفصولاً عن المعلّم (هجرات 0019–0038، نسخة الوثيقة ٤): دور `admin` + `assertAdmin/assertInstructor` + قشرة `AdminShell` موبايل-أوّلاً. **قمع التسجيل** (نموذجٌ ثلاثيّ المراحل، قائمة+تفاصيل بشريط مراحل، حجز/إعادة جدولة، اختبار تحديدٍ بالذكاء، **تقرير جلسةٍ تعريفية بالذكاء** يُرسَل عبر Resend، إنشاء حساباتٍ برابط دعوة + إسناد معلّم، تعديل/حذف/أرشفة/سجلّ نشاط). **مرحلة التعلّم** (اشتراكات + فواتير شهرية + انتقالات إيقاف/إلغاء + تقييمٌ دوريّ + حالات/شكاوى + لوحة مؤشّرات وقائمة «في خطر»). **المعلّمون** (قائمة + صفحة معلّمٍ بملفّ/أداء/طلاب). **نقل ملكية المواعيد للمعلّمة** (RLS تعزل كلّ معلّمٍ لمواعيده؛ المعلّمة تحرّر تفرّغها من `/studio/availability`، والإدارة تشاهد جدولها على شكل **تقويمٍ بمساحاتٍ لونية** للقراءة فقط). تسميات: «التسجيل»، اسم الحساب «الإدارة». **قرارٌ مثبّت:** موبايل-أوّلاً + ألوانٌ دلالية في كامل البناء.
- **2026-06-18 (Code):** أكملنا الميزات المتبقّية ونشرنا. **الإسناد لكل طالب** (`assignments`) + تضييق رؤية العناصر. **اختبار تحديد المستوى** (مولَّد، يُصحَّح على الخادم، يقترح مستوى — الأسئلة/الإجابات محجوبة). **الخطة الدراسية بالذكاء** (مولَّدة، تُعتمَد، «Add objectives» تربط التحديد→الخطة→الأهداف). **مسوّدة تقرير الجلسة بالذكاء**. **تصلُّب:** فرض الموافقة كبوّابة على التدريب (`current_learner_consented`)، تسجيلٌ محميّ عبر `signUp`، حدّ أبناءٍ لكل وليّ أمر. **مراجعة أمنية ذاتية** (لا anon، لا تصعيد أدوار، كل استدعاء service-role محميّ). **نُشِر التطبيق على Vercel (fra1) حيّاً على ward.academy** — `/studio` `/learn` `/guardian` `/signup` تعمل. (15 هجرة إجمالاً.)
- **2026-06-17 (Code — جلسة الباك-إند):** بُني الأساس كاملاً على Supabase/Frankfurt بـ **١٢ هجرة** مُختبَرة بعزلٍ حقيقي: العزل والهوية + خطّاف JWT · أولياء الأمور/الطلاب + الموافقة + رؤيةٌ بالدور · محرّك المحتوى (أهداف/عناصر/بوّابة اعتماد/فصل مفتاح الإجابة/إسناد) · الإرسالات + التقدّم المتزامن · الجلسات + التقارير · خدمة توليدٍ واحدة (عناصر + مسوّدات تقارير). بُنيت مساحات `/studio` `/learn` `/guardian` `/signup` بدخولٍ حسب الدور. **مراجعةٌ أمنية ذاتية لـ RLS:** لا تصعيد أدوار، الكتابة إمّا بدور المعلّم أو بخدمةٍ موثوقة، `anon` بلا وصول، عزل مستأجرٍ على كل جدول. أدوات `db:migrate/status/seed`. (نسخة الوثيقة ٣.)
- **2026-06-17 (Code):** إعادة بناء صفحة الهبوط لتكون مقودةً بالمنتج (إطلالة من الداخل، تقرير التفتّح، لوحة المعلّم، النتائج، الأسعار، الآراء، FAQ) + محاكاة شاشات وليّ الأمر/الطالب (أسماء أولى فقط). ضبط: أعمار ٩–١٣، أرقام غربية، إزالة الإيموجي، تخفيف خطاب الذكاء للأهل، أزرار حجز خضراء، إصلاحات الوردة على الجوّال (تفتّح عند الظهور + خلفية ممتدّة). حذف صفحتَي `/teacher` و`/brand` ومكوّن `Mascot`. تحديث هذه الوثيقة (نسخة ٢).

---
*يُحدّثه Claude Code بعد كل جلسة — حدّث «الحالة الحالية» و«سجلّ الجلسات».*
